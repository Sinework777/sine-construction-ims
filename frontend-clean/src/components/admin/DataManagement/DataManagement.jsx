import React from 'react';
import { Box, Typography } from '@mui/material';

export default function DataManagement() {
  return (
    <Box>
      <Typography variant="h5" fontWeight={700}>Data Management & Backup</Typography>
      {/* TODO: Add export/import, backup, restore, and UI */}
    </Box>
  );
}
