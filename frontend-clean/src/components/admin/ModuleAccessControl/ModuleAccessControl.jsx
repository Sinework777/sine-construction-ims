import React from 'react';
import { Box, Typography } from '@mui/material';

export default function ModuleAccessControl() {
  return (
    <Box>
      <Typography variant="h5" fontWeight={700}>Module Access Control</Typography>
      {/* TODO: Add access control management, role assignment, and UI */}
    </Box>
  );
}
