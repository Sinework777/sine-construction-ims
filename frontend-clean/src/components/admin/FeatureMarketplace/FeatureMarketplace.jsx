import React from 'react';
import { Box, Typography } from '@mui/material';

export default function FeatureMarketplace() {
  return (
    <Box>
      <Typography variant="h5" fontWeight={700}>Module Marketplace</Typography>
      {/* TODO: Add marketplace listing, install, and UI */}
    </Box>
  );
}
