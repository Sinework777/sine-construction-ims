import React from 'react';
import { Box, Typography } from '@mui/material';

export default function SupportAnnouncements() {
  return (
    <Box>
      <Typography variant="h4" fontWeight={700} mb={2}>Support & Announcements</Typography>
      <Typography variant="body1">Access support resources and view platform-wide announcements.</Typography>
    </Box>
  );
}
