import React from 'react';
import { Box, Typography } from '@mui/material';

export default function Support() {
  return (
    <Box>
      <Typography variant="h5" fontWeight={700}>Support & Helpdesk</Typography>
      {/* TODO: Add support ticket system, FAQ, and UI */}
    </Box>
  );
}
