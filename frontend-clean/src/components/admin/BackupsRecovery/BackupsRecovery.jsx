import React from 'react';
import { Box, Typography } from '@mui/material';

export default function BackupsRecovery() {
  return (
    <Box>
      <Typography variant="h4" fontWeight={700} mb={2}>Backups & Disaster Recovery</Typography>
      <Typography variant="body1">Manage backups, recovery points, and disaster recovery plans.</Typography>
    </Box>
  );
}
